name: Deploy to Hostinger
on:
  push:
    branches: [main]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Copy .htaccess
        run: cp public/.htaccess dist/ || true # Allows workflow to continue if .htaccess is missing
      - name: List dist contents
        run: ls -la dist/ # Debug: Check what’s in dist/
      - name: Deploy to Hostinger
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 77.37.34.75 # Replace with hostname (e.g., srv123456.hostinger.com) if needed
          username: u966234716
          password: Believe07!!
          port: 21
          protocol: ftp
          local-dir: ./dist/
         server-dir: /domains/autolinium.com/public_html/
          log-level: verbose # Debug: Detailed FTP logs
          exclude: |
            **/.git*
            **/node_modules*
