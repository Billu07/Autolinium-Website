name: Deploy to Hostinger
on:
  push:
    branches: [main]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Copy .htaccess (if you added it)
        run: cp public/.htaccess dist/ # Skip if you don't have this
      - name: Deploy to Hostinger
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 77.37.34.75 # e.g., srv123456.hostinger.com or 185.123.45.67
          username: u966234716
          password: Believe07!!
          port: 21 # Explicitly set for plain FTP
          protocol: ftp # Ensures plain FTP (not FTPS/SFTP)
          local-dir: ./dist/ # Note the ./ for relative path
          server-dir: public_html/
          exclude: | # Optional: Exclude unnecessary files
            **/.git*
            **/node_modules*
